let name = prompt("Введите свое имя");
let date_of_birth = prompt("Введите дату рождения");
let now = prompt("Какой сейчас год?");
let age = date_of_birth - now;
alert(name + "-" + age);
// if (date_of_birth <== now) {

// }
